import React from "react";

const CertificationsIcon = ({ customClassName }) => {
  return (
    <span className={`material-symbols-outlined ${customClassName}`}>
      workspace_premium
    </span>
  );
};

export default CertificationsIcon;
