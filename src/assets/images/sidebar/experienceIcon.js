import React from "react";

const ExperienceIcon = ({ customClassName }) => {
  return (
    <span className={`material-symbols-outlined ${customClassName}`}>
      work_history
    </span>
  );
};

export default ExperienceIcon;
