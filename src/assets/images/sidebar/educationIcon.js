import React from "react";

const EducationIcon = ({ customClassName }) => {
  return (
    <span className={`material-symbols-outlined ${customClassName}`}>
      school
    </span>
  );
};

export default EducationIcon;
