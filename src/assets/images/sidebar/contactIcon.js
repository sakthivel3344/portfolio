import React from "react";

const ContactIcon = ({ customClassName }) => {
  return (
    <span className={`material-symbols-outlined ${customClassName}`}>
      perm_contact_calendar
    </span>
  );
};

export default ContactIcon;
