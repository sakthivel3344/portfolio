import React from "react";

const ProjectsIcon = ({ customClassName }) => {
  return (
    <span className={`material-symbols-outlined ${customClassName}`}>work</span>
  );
};

export default ProjectsIcon;
