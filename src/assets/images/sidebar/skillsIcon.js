import React from "react";

const SkillsIcon = ({ customClassName }) => {
  return (
    <span className={`material-symbols-outlined ${customClassName}`}>
      checklist
    </span>
  );
};

export default SkillsIcon;
